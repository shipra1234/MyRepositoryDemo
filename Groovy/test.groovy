if('test')
 {
 
    println("test evaluated to value inside if")
  }
  if("test")
   {
     println("test evaluated to value inside if")
    }
    
    if(null)
    {
      println("null evaluated to value inside if")
    }
    if('null')
    {
     println("sec null evaluated to value inside if")
    }
    if(100)
    {
     println("100 evaluated to value inside if")
    }
    if(0)
    {
      println("0 evaluated to value inside if")
    }
    List[] l=null
    if(l)
     {
       println("list evaluated to value inside if")
      }
      List al=new ArrayList()
      
      if(al)
      {
       println("arraylist evaluated to value inside if")
      }
       